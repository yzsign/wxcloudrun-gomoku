<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gomoku.sync.mapper.RoomChatReportMapper">

    <insert id="insert">
        INSERT INTO room_chat_report (room_id, message_id, reporter_user_id, reason)
        VALUES (#{roomId}, #{messageId}, #{reporterUserId}, #{reason})
    </insert>
</mapper>
