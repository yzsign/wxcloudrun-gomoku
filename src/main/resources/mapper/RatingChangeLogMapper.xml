<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gomoku.sync.mapper.RatingChangeLogMapper">

    <insert id="insert">
        INSERT INTO rating_change_log (user_id, room_id, opponent_user_id, elo_before, elo_after, delta,
                                       total_steps, runaway)
        VALUES (#{userId}, #{roomId}, #{opponentUserId}, #{eloBefore}, #{eloAfter}, #{delta},
                #{totalSteps}, #{runaway})
    </insert>
</mapper>
